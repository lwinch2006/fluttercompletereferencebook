name: date_plugin_ios
description: "A new Flutter plugin project."
version: 0.0.1
homepage:

environment:
  sdk: ^3.13.0

dependencies:
  flutter:
    sdk: flutter
  date_plugin_platform_interface:
    path: ../date_plugin_platform_interface

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
flutter:
  plugin:
    platforms:
      ios:
        pluginClass: DatePluginIosPlugin